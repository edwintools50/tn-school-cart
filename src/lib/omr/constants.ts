// Kept separate from access.ts (which is "server-only") purely so client
// components can import this id too — e.g. to special-case single-seat
// quantity handling in the cart UI.
export const OMR_SUITE_PRODUCT_ID = "cmsbv6l350000fwrvlz6lnjhm";
